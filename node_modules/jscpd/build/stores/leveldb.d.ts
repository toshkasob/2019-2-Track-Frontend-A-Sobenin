import { IStoreOptions } from '../interfaces/store/store-options.interface';
import { IStoreValue } from '../interfaces/store/store-value.interface';
import { IStore } from '../interfaces/store/store.interface';
export declare class LevelDbStore<TValue extends IStoreValue> implements IStore<TValue> {
    private options;
    private db;
    constructor(options: IStoreOptions);
    get(key: string): Promise<TValue>;
    getAllByKeys(keys: string[]): Promise<TValue[]>;
    set(key: string, value: TValue): Promise<TValue>;
    init(): Promise<any>;
    has(key: string): Promise<boolean>;
    hasKeys(keys: string[]): Promise<boolean[]>;
    connect(): Promise<any>;
    delete(key: string): Promise<any>;
    update(key: string, value: TValue): Promise<any>;
    close(): Promise<any>;
}
