import { IStoreOptions } from '../interfaces/store/store-options.interface';
import { IStoreValue } from '../interfaces/store/store-value.interface';
import { IStore } from '../interfaces/store/store.interface';
import { MemoryStore } from './memory';
export declare class FilesStore<TValue extends IStoreValue> extends MemoryStore<TValue> implements IStore<TValue> {
    private options;
    protected values: {
        [key: string]: TValue;
    };
    private pathToFile;
    constructor(options: IStoreOptions);
    connect(): Promise<any>;
    init(values: {
        [p: string]: TValue;
    }): Promise<any>;
    close(): Promise<any>;
}
