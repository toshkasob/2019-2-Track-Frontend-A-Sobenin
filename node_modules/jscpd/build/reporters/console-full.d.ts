import { IClone, IOptions, IReporter } from '..';
import { JscpdEventEmitter } from '../events';
import { ConsoleReporter } from './console';
export declare class ConsoleFullReporter extends ConsoleReporter implements IReporter {
    constructor(options: IOptions);
    attach(eventEmitter: JscpdEventEmitter): void;
    report(clones: IClone[]): void;
    protected skipSource(source: any): void;
    private cloneFullFound;
}
