import { IReporter } from '..';
import { JscpdEventEmitter } from '../events';
export declare class TimeReporter implements IReporter {
    attach(eventEmitter: JscpdEventEmitter): void;
    report(): void;
}
