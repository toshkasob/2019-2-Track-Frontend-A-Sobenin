import { IClone, IOptions, IReporter } from '..';
import { JscpdEventEmitter } from '../events';
import { IStatistic } from '../interfaces/statistic.interface';
export declare class ConsoleReporter implements IReporter {
    protected options: IOptions;
    constructor(options: IOptions);
    attach(eventEmitter: JscpdEventEmitter): void;
    report(...args: [any, IStatistic]): void;
    protected cloneFound(clone: IClone): void;
    private convertStatisticToArray;
}
