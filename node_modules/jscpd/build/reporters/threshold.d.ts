import { IOptions, IReporter } from '..';
import { JscpdEventEmitter } from '../events';
export declare class ThresholdReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    attach(eventEmitter: JscpdEventEmitter): void;
    report(): void;
    private finish;
}
